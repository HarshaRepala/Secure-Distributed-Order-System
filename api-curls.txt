############################
# AUTH SERVICE
############################

# Register USER
curl -X POST "http://<xx.xx.xx.xx>/auth/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email":"user@example.com",
    "password":"Password@123",
    "role":"USER"
  }'

# Register ADMIN
curl -X POST "http://<xx.xx.xx.xx>/auth/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email":"admin@example.com",
    "password":"Admin@123",
    "role":"ADMIN"
  }'

# Login USER
curl -X POST "http://<xx.xx.xx.xx>/auth/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email":"user@example.com",
    "password":"Password@123"
  }'

# Refresh Token
curl -X POST "http://<xx.xx.xx.xx>/auth/api/v1/auth/refresh" \
  -H "Content-Type: application/json" \
  -d '{
    "refreshToken":"<REFRESH_TOKEN>"
  }'

# Logout
curl -X POST "http://<xx.xx.xx.xx>/auth/api/v1/auth/logout" \
  -H "Content-Type: application/json" \
  -d '{
    "refreshToken":"<REFRESH_TOKEN>"
  }'

# Health Check
curl "http://<xx.xx.xx.xx>/auth/actuator/health"

# Info
curl "http://<xx.xx.xx.xx>/auth/actuator/info"

############################
# ORDER SERVICE
############################

# Create Order
curl -X POST "http://<xx.xx.xx.xx>/order/api/v1/orders" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "productId":1,
    "quantity":2
  }'

# Get Orders
curl -X GET "http://<xx.xx.xx.xx>/order/api/v1/orders" \
  -H "Authorization: Bearer <ACCESS_TOKEN>"

# Cancel Order
curl -X POST "http://<xx.xx.xx.xx>/order/api/v1/orders/<ORDER_ID>/cancel" \
  -H "Authorization: Bearer <ADMIN_ACCESS_TOKEN>"

# Health Check
curl "http://<xx.xx.xx.xx>/order/actuator/health"

# Info
curl "http://<xx.xx.xx.xx>/order/actuator/info"

############################
# INVENTORY SERVICE
############################

# Reserve Stock
curl -X POST "http://<xx.xx.xx.xx>/inventory/api/v1/inventory/reserve" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "productId":1,
    "quantity":2
  }'

# Release Stock
curl -X POST "http://<xx.xx.xx.xx>/inventory/api/v1/inventory/release" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "productId":1,
    "quantity":1
  }'

# Update Stock
curl -X POST "http://<xx.xx.xx.xx>/inventory/api/v1/inventory/update" \
  -H "Authorization: Bearer <ADMIN_ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "productId":1,
    "name":"Mechanical Keyboard",
    "quantity":100
  }'

# Health Check
curl "http://<xx.xx.xx.xx>/inventory/actuator/health"

# Info
curl "http://<xx.xx.xx.xx>/inventory/actuator/info"